import React from "react";

const AboutSectionEmpty = () => {
    return (
        <div
            className="col-wrapper bloc-medium bloc-duo"
            style={{
                padding: 0,
            }}
        ></div>
    );
};

export default React.memo(AboutSectionEmpty);
